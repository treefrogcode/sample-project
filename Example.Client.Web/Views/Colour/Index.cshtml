@model string

@{
    ViewBag.Title = "Colour Page";
}

<div id="react-content"></div>

@section scripts
{
    <script type="text/javascript">
        ReactDOM.render(
            React.createElement(AdminPage, { initialData: '@Html.Raw(Model)', type: 'Colour' }),
            document.getElementById('react-content')
        );
    </script>
}
